{
  "name": "jaipur-server",
  "version": "0.1.0",
  "description": "The Jaipur Server",
  "main": "index.js",
  "scripts": {
    "start": "babel-node --presets es2015 src/index.js",
    "build": "babel --presets es2015 -d build/ src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Bell",
  "license": "WTFPL",
  "dependencies": {
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
